<Page
    x:Class="FitAppMVVM.Presentation.WorkoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FitAppMVVM.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView ItemsSource="{Binding Workouts}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text="{Binding Notes}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <StackPanel Grid.Row="1"
           HorizontalAlignment="Center"
           VerticalAlignment="Center"
           Spacing="16">


            <TextBox Text="{Binding Name, Mode=TwoWay}" PlaceholderText="Enter the name:" />
            <TextBox Text="{Binding Notes, Mode=TwoWay}" PlaceholderText="Enter a note:" />
            <Button Content="Add A Workout" Command="{Binding AddWorkoutCommand}" />
            
        </StackPanel>
       

    </Grid>
</Page>
